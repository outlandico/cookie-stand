let geno = {
    name: "Geno",
    hair: "short",
    colors: "white"
}

// returns a random...
function getRandomNumberBetween(min,max){
    return Math.floor( Math.random) * (max-min+1)
}
Geolocation.getAge();
console.log(geno.age);
geno.speak();
geno.walk();